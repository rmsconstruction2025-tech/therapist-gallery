rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /therapists/{allPaths=**} {
      // Anyone can read (images served in gallery)
      allow read: if true;
      // Only authenticated admin can write
      allow write: if request.auth != null;
    }
  }
}
